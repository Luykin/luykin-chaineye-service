<div class="auth-info">
  <span>
    🔐 已认证访问 - <%= typeof user !== 'undefined' && user ? user.name : '用户'
    %>
    <span style="color: #999; font-size: 0.9em"
      >[<%= typeof user !== 'undefined' && user ? user.role : 'unknown'
      %>]</span
    >
    - 统计数据面板 [server-byteplus]
  </span>
  <a href="/api/xhunt/stats/logout" class="logout-btn">退出登录</a>
</div>
