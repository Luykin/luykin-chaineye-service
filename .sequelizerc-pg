const path = require("path");

module.exports = {
  config: path.resolve("config", "config-pg.json"),
  "models-path": path.resolve("src", "xhunt", "models"),
  "seeders-path": path.resolve("seeders"),
  "migrations-path": path.resolve("migrations-pg"),
};
